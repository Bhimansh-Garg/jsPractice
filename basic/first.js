console.log("hello World!")

const num = 12
let rollno = 10
var name = "john doe"
hello= "hello world"

console.log(rollno)
console.table({num,rollno, name, hello})

/* pref to use let as var cause problems with scope and hoisting. const is used for values that should not be reassigned. */
